mutation CreateGroup($name: String!) {
    createGroup(
      options: { name: $name }
    ) {
      errors {
        field
        message
      }
      group {
        id
        name
      }
    }
  }