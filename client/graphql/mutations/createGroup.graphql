mutation CreateGroup($name: String!, $description: String!, $member: Int!) {
    createGroup(
      options: { name: $name, description: $description, member: $member }
    ) {
      errors {
        field
        message
      }
      group {
        id
        name
        description
        member
      }
    }
  }