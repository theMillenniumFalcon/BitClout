query Group($id: Int!) {
    group(id: $id) {
        id
        createdAt
        updatedAt
        name
        description
        membersNumber
        creatorId
        posts {
            id
            title
            text
        }
    }
}