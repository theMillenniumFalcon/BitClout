query Group($id: Int!) {
    group(id: $id) {
        id
        createdAt
        updatedAt
        name
        description
        membersnumber
        creatorId
        posts {
            id
            title
            text
        }
        members {
            userId
        }
    }
}