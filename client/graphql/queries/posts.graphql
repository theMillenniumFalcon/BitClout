query Posts {
  posts {
      id
      createdAt
      updatedAt
      title
      points
      voteStatus
      creator {
        id
        username
      }
      group {
        id
        name
        description
      }
  }
}
